{
  "name": "a4pm-masterchef",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w packages/api run build && npm -w packages/web run build",
    "dev": "npm -w packages/api run start:dev",
    "dev:web": "npm -w packages/web run serve",
    "lint": "npm -w packages/api run lint && npm -w packages/web run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}